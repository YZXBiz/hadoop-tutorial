<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>

<configuration>
    <!-- Hive Metastore DB connection -->
    <property>
        <name>javax.jdo.option.ConnectionDriverName</name>
        <value>org.mariadb.jdbc.Driver</value>
    </property>

    <property>
        <name>javax.jdo.option.ConnectionURL</name>
        <value>jdbc:mysql://mariadb:3306/hive_metastore</value>
    </property>

    <property>
        <name>javax.jdo.option.ConnectionUserName</name>
        <value>hive</value>
    </property>

    <property>
        <name>javax.jdo.option.ConnectionPassword</name>
        <value>hive</value>
    </property>

    <!-- Hive Metastore server -->
    <property>
        <name>hive.metastore.uris</name>
        <value>thrift://hive-metastore:9083</value>
    </property>

    <property>
        <name>hive.metastore.schema.verification</name>
        <value>false</value>
    </property>

    <!-- HiveServer2 settings -->
    <property>
        <name>hive.server2.thrift.port</name>
        <value>10000</value>
    </property>

    <property>
        <name>hive.server2.thrift.bind.host</name>
        <value>0.0.0.0</value>
    </property>

    <property>
        <name>hive.server2.webui.port</name>
        <value>10002</value>
    </property>

    <!-- Warehouse directory -->
    <property>
        <name>hive.metastore.warehouse.dir</name>
        <value>/user/hive/warehouse</value>
    </property>

    <!-- Execution engine -->
    <property>
        <name>hive.execution.mode</name>
        <value>distributed</value>
    </property>

    <!-- Hive execution engine (MapReduce) -->
    <property>
        <name>hive.execution.engine</name>
        <value>mr</value>
    </property>

    <!-- Enable temporary file cleanup -->
    <property>
        <name>hive.exec.parallel</name>
        <value>true</value>
    </property>

    <property>
        <name>hive.exec.parallel.thread.number</name>
        <value>8</value>
    </property>

    <!-- Show current database in prompt -->
    <property>
        <name>hive.cli.print.current.db</name>
        <value>true</value>
    </property>

    <!-- Change header format -->
    <property>
        <name>hive.cli.print.header</name>
        <value>true</value>
    </property>

    <!-- Allow multiple sessions -->
    <property>
        <name>hive.session.impl.withhook</name>
        <value>org.apache.hadoop.hive.ql.session.SessionState</value>
    </property>

    <!-- Compression settings -->
    <property>
        <name>hive.exec.compress.intermediate</name>
        <value>false</value>
    </property>

    <property>
        <name>hive.exec.compress.output</name>
        <value>false</value>
    </property>

    <!-- Statistics -->
    <property>
        <name>hive.stats.autogather</name>
        <value>true</value>
    </property>

    <property>
        <name>hive.compute.query.using.stats</name>
        <value>true</value>
    </property>

    <!-- Logging -->
    <property>
        <name>hive.querylog.location</name>
        <value>/tmp/hive/logs</value>
    </property>

    <!-- Fetch task optimization -->
    <property>
        <name>hive.fetch.task.conversion</name>
        <value>more</value>
    </property>

    <!-- Memory settings -->
    <property>
        <name>hive.mapred.reduce.tasks.speculative.execution</name>
        <value>true</value>
    </property>

    <!-- Null ordering -->
    <property>
        <name>hive.nullsort.order</name>
        <value>a</value>
    </property>
</configuration>
